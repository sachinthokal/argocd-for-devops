kind: ConfigMap
apiVersion: v1

metadata:
  name: cicd-compass-configmap
  namespace: compass

data:
  application.properties: |
    # Application properties for CICD Compass
    app.name=cicd-compass-app-prod
    app.environment=production-k8s
    app.version=v1.0.1
    app.region=us-east-1
    app.buildNumber=build-1799